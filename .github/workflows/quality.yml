name: Quality

on:
  workflow_call:

permissions:
  contents: read

env:
  CI: "true"
  GOWORK: "off"
  PNPM_VERSION: 11.18.0
  STATICCHECK_VERSION: "2026.2.1"
  ACTIONLINT_VERSION: v1.7.12

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.18.0

      - name: Set up pnpm
        shell: bash
        run: |
          corepack enable
          corepack prepare "pnpm@${PNPM_VERSION}" --activate

      - name: Resolve pnpm store
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: |
            pnpm-${{ runner.os }}-${{ runner.arch }}-

      - name: Install frontend dependencies
        working-directory: frontend
        run: pnpm install --frozen-lockfile

      - name: Check frontend formatting
        working-directory: frontend
        run: pnpm run format:check

      - name: Check frontend types
        working-directory: frontend
        run: pnpm run check

      - name: Run frontend tests
        working-directory: frontend
        run: pnpm run test

      - name: Build embedded frontend assets
        working-directory: frontend
        run: pnpm run build

  go:
    name: Go and workflows
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes \
            build-essential \
            libgtk-4-dev \
            libwebkitgtk-6.0-dev \
            pkg-config

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Create frontend embed placeholder
        run: |
          mkdir -p frontend/dist
          touch frontend/dist/index.html

      - name: Cache Go analysis tools
        id: analysis-tools-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/go/bin/actionlint
            ~/go/bin/staticcheck
          key: analysis-tools-${{ runner.os }}-${{ runner.arch }}-${{ env.STATICCHECK_VERSION }}-${{ env.ACTIONLINT_VERSION }}

      - name: Install Go analysis tools
        if: steps.analysis-tools-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          go install "honnef.co/go/tools/cmd/staticcheck@${STATICCHECK_VERSION}"
          go install "github.com/rhysd/actionlint/cmd/actionlint@${ACTIONLINT_VERSION}"

      - name: Run Go tests
        run: go test ./... -count=1

      - name: Run Go race tests
        if: github.event_name == 'push'
        run: go test -race ./... -count=1

      - name: Run go vet
        run: go vet ./...

      - name: Run Staticcheck
        run: staticcheck ./...

      - name: Lint GitHub Actions workflows
        run: actionlint
